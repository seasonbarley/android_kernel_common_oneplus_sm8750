name: Makefile CI

on:
  push:
    branches: [ "oneplus/sm8750_v_15.0.0_oneplus_13" ]
  pull_request:
    branches: [ "oneplus/sm8750_v_15.0.0_oneplus_13" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
